# 本地开发配置（将本文件复制为 application-local.yml，填入你的 key）
# application-local.yml 已被 .gitignore 忽略，不会提交到仓库
spring:
  ai:
    dashscope:
      api-key: your-aliyun-api-key

search-api:
  api-key: your-search-api-key（可选）
